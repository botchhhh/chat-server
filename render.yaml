services:
  - type: web
    name: ai-chatbot
    env: node
    branch: main
    plan: free
    buildCommand: npm install
    startCommand: node server/server.js
    envVars:
      - key: PUTER_AUTH_TOKEN
        fromSecret: puter_auth_token
      - key: PORT
        value: 10000
